#!/bin/bash
# Pre-tool hook: runs before each tool call in CCGS.
# HMGS equivalent: skill pre-conditions are checked at the start of each skill.
# This script is provided for compatibility with Claude Code's hook system.

echo "[HMGS] pre-tool: checking pre-conditions..."
# In HMGS, pre-conditions are documented in each skill's SKILL.md
